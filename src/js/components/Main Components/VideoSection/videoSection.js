import React, {useState} from 'react';

function videoSection() {
    return (
        <section className={'dupa'}>
            <div className={'wrapper'} style={{color:'white', fontSize: '48px'}}>Tu będą video w HD</div>
        </section>
    )
}

export default videoSection;